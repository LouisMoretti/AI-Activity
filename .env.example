# Copy to .env and adjust. Never commit .env.
PORT=3000
# Shared viewer password for the testing phase (multi-user login comes later).
# Leave empty to disable viewer auth on localhost.
DASHBOARD_PASSWORD=
# Path to the SQLite database file.
DB_PATH=./data/dashboard.db
